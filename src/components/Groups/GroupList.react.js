import React from "react";




function GroupList(props: Props) {

    const {groupList} = props;
    
    return (
        <div>
            groupList.map(group, ());
        </div>
    );

}

export default GroupList;
