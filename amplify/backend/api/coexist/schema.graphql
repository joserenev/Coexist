type User @model {
  id: ID!
  username: String!
  email: String!
  name: String
  phone: String
  group: Group @connection(name: "GroupUsers")
}

type Group @model {
  id: ID!
  name: String
  users: [User] @connection(name: "GroupUsers")
}
